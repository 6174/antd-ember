{{input 
	class=inputClass 
	disabled=disabled
	type=type
	value=value  
	role="input"
	required=required
	max=max
	min=min
	data-error=dataError
	maxlength=maxlength
	minlength=minlength
	pattern=pattern
	placeholder=placeholder
}}